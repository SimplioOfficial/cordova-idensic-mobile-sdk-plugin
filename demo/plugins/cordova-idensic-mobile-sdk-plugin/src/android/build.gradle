buildscript {
    apply from: rootProject.file("dependencies.gradle")
    repositories {
        google()
        jcenter()
        mavelCentral()
    }
    dependencies {
        classpath deps.build.androidPlugin
        classpath deps.build.kotlinPlugin
        classpath deps.build.butterknifePlugin
    }
}

plugins {
    id "maven"
}

apply plugin: 'com.android.library'

sourceCompatibility = 1.8
targetCompatibility = 1.8

rootProject.allprojects {
    repositories {
        google()
        jcenter()
        maven { url "https://maven.sumsub.com/repository/maven-public/" }
    }
}

android {
    compileSdkVersion 30
    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 30
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    implementation "com.sumsub.sns:idensic-mobile-sdk:1.18.3"
    implementation "com.sumsub.sns:idensic-mobile-sdk-prooface:1.18.3"
    implementation 'com.google.android.material:material:1.3.0'
    implementation 'org.apache.cordova:framework:8.1.0'
}
